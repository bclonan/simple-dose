# Public browser providers work without an API key.
# Optional server-side tooling only. Never prefix a secret with VITE_.
OPENFDA_API_KEY=
PORT=4179
# Optional deterministic build. The production default is hybrid.
# VITE_CLEARDose_DATA_MODE=demo
#Census key
CENSUS_API_KEY=
